name: dog_walker_app
description: PawPal - Dog Walking Service App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Firebase
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9
  firebase_messaging: ^15.2.7

  # State Management
  provider: ^6.1.2

  # Location & Maps
  geolocator: ^13.0.1
  google_maps_flutter: ^2.9.1

  # UI & Media
  image_picker: ^1.1.2

  # Storage & Preferences
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  sqflite: ^2.3.3+2

  # Permissions
  permission_handler: ^11.3.1

  # Utilities
  intl: ^0.20.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Assets
  # assets:
  #   - images/
